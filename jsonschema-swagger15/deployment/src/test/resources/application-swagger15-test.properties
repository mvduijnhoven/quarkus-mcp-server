# Quarkus MCP Server Swagger 1.5 Test Properties
# For these tests, we programmatically set the 'enable' flag.
# So, the value here (if any) would be overridden by the test logic.
# quarkus.mcp.server.jsonschema.swagger15.enable=true
