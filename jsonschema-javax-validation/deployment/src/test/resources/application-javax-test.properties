# Quarkus MCP Server Javax Validation Test Properties
# Default values from JavaxValidationBuildTimeConfig will be used if not overridden here.
# For this test, we rely on defaults and then programmatically change config in the test itself
# to verify different scenarios.
# Example:
# quarkus.mcp.server.jsonschema.javax-validation.not-nullable-field-is-required=true
